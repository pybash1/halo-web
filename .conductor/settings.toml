#:schema https://conductor.build/schemas/settings.repo.schema.json

file_include_globs = ".env\n"

[scripts]
setup = "bun install --frozen-lockfile"
run = "bun run dev -- --port $CONDUCTOR_PORT"
archive = "rm -rf node_modules dist .astro .vercel"
run_mode = "concurrent"
