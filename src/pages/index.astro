---
import "../styles/global.css";
import "@fontsource/instrument-serif";
import "@fontsource-variable/dm-sans";
import WaitlistButton from "../components/WaitlistButton";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import ScrollVideoBackground from "../components/ScrollVideoBackground";
import MissionStories from "../components/MissionStories";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preload" href="/cursor-default.svg" as="image" />
    <link rel="preload" href="/cursor-pointer.svg" as="image" />
    <link rel="preload" href="/cursor-text.svg" as="image" />
    <title>Halo</title>
  </head>
  <body id="top" class="has-custom-cursor font-sans bg-black text-white">
    <ScrollVideoBackground client:load />

    <main class="relative z-20">
      <Navbar />
      <!-- Content Sections Below Video -->
      <div>
        <!-- Hero CTA Section -->
        <div id="hero-section" class="h-svh w-full flex items-center justify-center px-4 md:px-10">
          <div id="hero-content" class="max-w-5xl w-full text-center space-y-16">
            <div class="space-y-6">
              <h2 class="text-5xl md:text-[5.5rem] font-serif leading-[1.1] tracking-tight">
                Because Your <br class="hidden md:block"/> Safety Matters
              </h2>
              <p class="text-xl md:text-2xl text-white/60 font-light max-w-xl mx-auto leading-relaxed">
                Giving you the confidence to move <br class="hidden md:block"/> 
                through the world without hesitation.
              </p>
            </div>
            
            <div class="relative inline-flex items-center justify-center">
              <!-- Glowing layers hugging the borders -->
              <div class="absolute -inset-[3px] rounded-xl bg-white/90 blur-md animate-pulse" style="animation-duration: 2s;"></div>
              <div class="absolute -inset-[10px] rounded-xl bg-white/60 blur-xl animate-pulse" style="animation-duration: 3s; animation-delay: 0.5s;"></div>
              <div class="absolute -inset-[20px] rounded-xl bg-white/30 blur-2xl animate-pulse" style="animation-duration: 4s; animation-delay: 1s;"></div>
              
              <WaitlistButton
                className="relative bg-white text-black py-4 px-10 text-lg md:text-xl font-bold rounded-xl shadow-[0_0_25px_rgba(255,255,255,0.8)] hover:shadow-[0_0_50px_rgba(255,255,255,1)] hover:scale-[1.03] active:scale-95 transition-all duration-300 font-serif inline-block cursor-pointer"
                client:load
                isHeroButton={true}
              >
                Make the world safer
              </WaitlistButton>
            </div>
          </div>
        </div>

        <!-- Spacer for blank blue screen -->
        <div class="h-[30vh] w-full pointer-events-none"></div>

        <!-- Mission Stories Section -->
        <div id="mission" class="relative z-20">
          <MissionStories client:load />
        </div>

        <!-- About Section -->
        <section id="about" class="min-h-[100svh] flex items-center justify-center px-4 md:px-10 py-20" style="background: transparent;">
          <div class="space-y-6 md:space-y-10 max-w-2xl">
            <h2 class="text-4xl md:text-6xl font-serif">About Us</h2>
            <p class="font-satoshi text-base md:text-lg leading-relaxed text-white/85">
              We are Ananjan and Ritankar, two friends who decided we could not just
              stand by while people we care about face real safety threats every day.
            </p>
            <p class="font-satoshi text-base md:text-lg leading-relaxed text-white/85">
              After hearing too many stories from friends and family about feeling
              unsafe walking alone or being in dangerous situations without help,
              we realized the tools out there were missing what people need in
              critical moments.
            </p>
            <p class="font-satoshi text-base md:text-lg leading-relaxed text-white/85">
              We are building Halo because everyone deserves to feel safe and have
              real help when they need it.
            </p>
          </div>
        </section>
      </div>
      <Footer />
    </main>
    <script>
      import Lenis from 'lenis';
      import gsap from 'gsap';
      import { ScrollTrigger } from 'gsap/ScrollTrigger';

      gsap.registerPlugin(ScrollTrigger);

      const lenis = new Lenis();

      lenis.on('scroll', ScrollTrigger.update);

      gsap.ticker.add((time) => {
        lenis.raf(time * 1000);
      });

      gsap.ticker.lagSmoothing(0);
    </script>
  </body>
</html>
